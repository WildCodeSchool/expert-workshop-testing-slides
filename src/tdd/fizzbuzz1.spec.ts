describe( 
    "Fizzbuzz test suite", 
    ()=>{
        it(
            "should be able to invoke the fizzbuzz function", 
            ()=>{
                expect(fizzbuzz).toBeDefined();
            }
        )
    }
);